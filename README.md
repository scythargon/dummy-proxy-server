```
$ ./proxy.py runserver -?
usage: proxy.py runserver [-?] [--host HOST] [--port PORT] [--site SITE]
                          [--cache]

optional arguments:
  -?, --help            show this help message and exit
  --host HOST, -h HOST
  --port PORT, -p PORT
  --site SITE, -s SITE
  --cache, -c
```
